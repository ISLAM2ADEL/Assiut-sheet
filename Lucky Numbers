#include<iostream>
#include<bits/stdc++.h>
#include<cmath>
#define ll long long
using namespace std ;

int main() {
    int x;
    cin>>x;
    if(x%10==0)
        cout<<"YES"<<endl;
    else if(((x/10)%(x%10)==0 || (x%10)%(x/10)==0))
        cout<<"YES"<<endl;
    else
        cout<<"NO"<<endl;
    return 0;
}
