#include<iostream>
#include<bits/stdc++.h>
#include<cmath>
#define ll long long
using namespace std ;

int main() {
    int x,y;
    cin>>x;
    for(int i=1;i<=x;i++){
        ll ans=1;
        cin>>y;
        for(int i=y;i>=2;i--)
            ans*=i;
        cout<<ans<<endl;
    }
    return 0;
}
