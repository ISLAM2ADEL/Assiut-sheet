#include<iostream>
#include<bits/stdc++.h>
#define ll long long
using namespace std ;
int main() {
    char x;
    cin>>x;
    int y=(int)x;
    if(y>=65 && y<=90)
        cout<<char(x+32);
    else if ( y>=97 && y<=122)
        cout<<char(x-32);
    return 0;
}
